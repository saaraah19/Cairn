# URL of the Cairn backend API (see server/.env.example)
VITE_API_BASE_URL=http://localhost:5000

# Google OAuth Client ID (same value as GOOGLE_CLIENT_ID in server/.env.example)
# The "Sign in with Google" button is hidden automatically if this is unset.
VITE_GOOGLE_CLIENT_ID=
